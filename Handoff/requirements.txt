# Python dependencies for the prediction scripts.
# Versions pinned to what the .pkl models were trained and verified against —
# scikit-learn in particular will warn or fail to unpickle across major versions.
# Requires Python 3.13.

scikit-learn==1.7.2
xgboost==3.1.3
pandas==2.3.3
numpy==2.3.3
scipy==1.16.2
joblib==1.5.2
