# ML packages the prediction scripts need.
#
# Versions are PINNED because the trained .pkl model files are tied to them.
# scikit-learn in particular will warn or fail to unpickle across major
# versions — upgrading without retraining will break predictions.
#
# Requires Python 3.13.
#
# Install both files:
#     pip install -r requirements.txt
#     pip install -r requirements-service.txt

scikit-learn==1.7.2
xgboost==3.1.3
pandas==2.3.3
numpy==2.3.3
scipy==1.16.2
joblib==1.5.2
